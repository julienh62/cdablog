{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}


<div class="container">
     <h1>List of Users</h1>
    
    <ul>
        {% for user in users %}
            <li>
        <h2>{{ user.name}}</h2>
        <img src="{{ user.avatar }}" alt="Avatar de {{ user.name }}" />
       {# <a href="{{ path('user_show', {'id': user.id}) }}" class="btn btn-primary">Voir détails du user</a> #}
    </li>
       {% endfor %}
    </ul>
</div>
{% endblock %}
